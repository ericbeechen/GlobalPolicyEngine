[project]
name = "policypath"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "databento>=0.86.0",
    "pandas>=3.0.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/policypath"]

[dependency-groups]
dev = [
    "pytest>=8.0",
    # used by config/meetings/pull_fomc.py and tests/data/build_fixtures.py,
    # neither of which runs in the test suite
    "requests>=2.31",
    "beautifulsoup4>=4.12",
]

[tool.pytest.ini_options]
testpaths = ["tests"]